name: streamyz
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.14.0
  firebase_auth: ^5.6.0
  google_fonts: ^6.2.1
  form_validator: ^2.1.1
  cloud_firestore: ^5.6.9
  flutter_slidable: ^4.0.0
  cached_network_image: ^3.4.1
  zego_zim: ^2.20.0+2
  zego_uikit_prebuilt_live_streaming: ^3.14.1
  
  # ZEGOCLOUD dependencies (versions compatibles)



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
